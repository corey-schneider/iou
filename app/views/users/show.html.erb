<div>
  <p>user: <%= @user.first_name %> <%= @user.last_name %>, id: <%= @user.id %></p>
  <p>Owned bills:</p>
  <% @user.bills.each do |bill| %>
    <%= link_to bill.title, bill, class: 'no-underline' %>
  <% end %>
</div>