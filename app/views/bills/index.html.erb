<p>bill index</p>
<%= link_to 'new bill', new_bill_path %>
<br>
<%= link_to 'home', root_path %>

<% if !@bills.nil? %>
  <% @bills.each do |bill| %>
    <p><%= bill.title %></p>
  <% end %>
<% end %>